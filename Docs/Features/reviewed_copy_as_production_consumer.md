# Reviewed `CopyAs` production consumer

> Status: bounded Native same-directory create-only consumer implemented and locally verified; private cross-volume staging foundation, destination stage, protected-root serialization, detached root admission, locked-session runner, lifecycle retention/inspection and publication barrier are implemented but unactivated

## Scope

`CopyAs::Perform` routes one regular Native item through the reviewed M3 lifecycle when the destination is an absolute path in the source directory, default Copy options preserve the supported semantics, and the provider returns path-specific `SameVolumeClone` eligibility. `CrossVolumeStaged` is a distinct future selection scope and requires a separate signed authority. Known unsupported shapes retain the established Copy operation. Unavailable eligibility blocks the request.

## Production flow

1. Capture the active pane identity, data generation, exact focused item and requested destination.
2. Resolve path-specific conditional-Copy eligibility without granting execution authority.
3. Build the exact structural plan and VFS-bound preflight on a worker.
4. Revalidate the captured pane and item, then show source, destination, create-only scope, conflict policy, file/byte estimate, destination space, access evidence and warnings.
5. On explicit approval, revalidate intent again, mint the single-use reviewed authority and acquire a window submission ticket.
6. Submit through the process-owned journal, run-receipt custodian, private reviewed factory, window `Pool` and provider transaction.
7. Present item status and the owning durable terminal outcome. Successful publication refreshes and focuses the destination; cancellation is silent; failure or uncertainty preserves typed publication, system, sync and recovery evidence.

Blocked, stale, cancelled, unpersisted or insufficiently bound reviewed intent never reaches `Pool`. Window close and application termination cancel new submissions and wait for acquired submission tickets. Recovery is process-owned and bounded to the exact plan: `Retry`, then same-storage reopen, `Reconcile`, and `ReleaseReconciled` when required. After confirmed terminal/interrupted evidence and any required successful release, a busy cold history projection offers one explicit refresh whose opaque state contains only storage identity plus a consumed bit; it invokes `RefreshColdHistory` only and cannot restart custody recovery. Startup exposes interrupted Copy history without automatic resume.

The test-only physical checkpoint harness exercises this production route through an injected `NativeHost` at two provider commit boundaries: before clone publication and after publication plus ordinary file/parent `fsync`, before `F_FULLFSYNC`. It durably writes a descriptor-anchored manifest before an optional operator hold. Its separate recovery profile validates the retained workspace and manifest, reads the unfinished journal through a read-only descriptor-bound inspector, and only then permits normal journal startup classification from `Running` to `Interrupted`. Its authority is limited to the retained-artifact evidence boundary; a subsequent process-owned recovery decision supplies any execution authority.

## Verified evidence

- reviewed CopyAs selection, unavailable fail-closed behavior, submission gate and app boundary: 10 / 98;
- app boundary: blocked, stale, unpersisted and cancelled paths make zero submission calls; the exact review projection and owning durable failure dispatch before Pool removal with generic completion suppressed pass 4 / 70 in Debug, Release ASAN and Release UBSAN;
- recovery coordinator: 6 / 67;
- fresh unsigned Debug `WinCommander-Unsigned` build passes;
- historical Debug `WinCommanderUT` snapshot: 309 / 313 cases and 4,994 / 4,998 assertions pass; four AppKit pasteboard cases reproducibly fail because the service is unavailable;
- historical Debug `OperationsUT` snapshot: 169 / 170 and 4,744 / 4,748 pass; one existing set-ID NativeCreateCopy metadata case fails on this host;
- aggregate ASAN and UBSAN both reach the same pasteboard baseline after BaseUT and ConfigUT without sanitizer diagnostics;
- historical full Debug `VFSUT` run: 95 / 43,566; latest Debug `VFSUT` records 161 / 164 cases and 46,560 / 46,563 assertions, with only the `Application marker`, `FetchUsers` and `FetchGroups` host baselines; latest full Debug `OperationsUT` records 203 / 204 and 5,379 / 5,383 with only the NativeCreateCopy set-ID metadata host baseline; latest full Debug `WinCommanderUT` records 330 / 334 and 5,317 / 5,321 with four headless pasteboard host baselines;
- ProviderCapabilities: 16 / 549; Native conditional Copy: 16 / 328.
- The following source-snapshot foundation entry is historical evidence from before destination-stage and protected-root-locking closure; the current stage/locking evidence and remaining gates are stated immediately after it.
- cross-volume authority foundation: rebuilt `VFSUT '*conditional Copy*' --rng-seed 424242` passes 26 / 850; focused `OperationsUT` provider/orchestrator/factory sets pass 44 / 1,608; rebuilt `WinCommanderUT 'reviewed CopyAs policy *' --rng-seed 424242` passes 11 / 101. The test seam sees only anchored FDs, a basename and complete scalar seals. The private V1 codec passes 4 / 74 and the injectable staging client passes 4 / 108, including FD duplication, exact correlation/lease binding, single terminal request, client-side cancellation gates before Begin, after Granted and before Commit dispatch, cleanup and conservative lost-reply handling. The helper's move-only `ValidatedBegin` exact-revalidates both duplicated descriptors and all scalar seals, rejects writable FD rights and sets/checks `FD_CLOEXEC`; its 4 / 208 VFSUT filter passes in Debug, Release ASAN and Release UBSAN. The bounded lease store now accepts only that validated authority, mints its opaque token with `SecRandomCopyBytes`, accepts one correlation per authenticated owner and closes descriptor rights on terminal take, revoke and rejected admission; its updated focused VFSUT filter passes 5 / 328. The protected-root ledger accepts an exact root-owned `0700` root FD and keeps one live owner per sealed root. `MaterializeEmptyAndSeal` retains the durable primary reservation, creates exactly one empty private artifact, syncs `fsync(artifact)` → `fsync(root)` → `F_FULLFSYNC(artifact)`, then appends a separate sealed companion manifest. Read-only reconciliation accepts an artifact only when that companion has the exact root/header/role/artifact-ID claims and its full no-follow seal matches; incomplete, malformed and mismatched state remains retained. The durable quota is 16 canonical primary reservation records, including malformed records, across helper restart, and a duplicate header/role record is rejected durably; its current focused Debug filter passes 13 / 319. `SourceSnapshotWriter` consumes only the private committed terminal lease, rejects Abort before cancellation/reservation, revalidates the exact read-only source descriptor at every copy-to-seal boundary, copies the reviewed byte size with positional `pread`/`pwrite` into one private `O_EXCL|O_NOFOLLOW` `0600` artifact, synchronizes it, append-seals V1 and returns only a move-only process-owned continuation with the terminal descriptor pair and read-only artifact FD. V1 persists the artifact seal, not source provenance or a restart-resumable continuation; restart reconciliation remains read-only. Its focused VFSUT filter passes 6 / 287 in Debug, Release ASAN and Release UBSAN, and the helper target builds in Debug. The helper dispatcher validates Begin but remains inert and cannot select this authority. NonMAS owns V1 embed/privileged-helper declarations and the target requests Developer ID plus hardened runtime; signed artifact/SMJobBless validation awaits a usable local signing identity. The private publication barrier is implemented below; helper-only cleanup/recovery, publisher consumption for exact destination namespace publication, signed transport and the two-internal-APFS fixture remain open.

- pre-runner private destination-stage closure supersedes the earlier historical statement that destination staging remained open: `SourceSnapshotWriter` admits only a source root on `request.source.device`; `DestinationStageWriter` consumes only its sealed Commit continuation and admits only a destination root on `request.destination_parent.device` with distinct source/destination devices. It revalidates the read-only snapshot, original source and destination-parent descriptors, root binding and exact reservation after every unlocked cancellation probe, then writes a private `O_EXCL|O_NOFOLLOW` `0600` one-link stage, synchronizes `artifact → root → F_FULLFSYNC`, append-seals V1 and returns only a move-only helper-private continuation. It neither publishes nor exposes a destination pathname or cleanup authority. The final consolidated filter passes 9 cases with one expected no-root device-fixture skip / 385 assertions in Debug, Release ASAN and Release UBSAN. The no-root run is not physical two-volume or cleanup evidence. The Debug helper target builds; the 152 / 45,372 full Debug `VFSUT` aggregate is pre-runner evidence.

- current protected-root PID/locking closure: `ProtectedRootLedger` opens its own exact helper-owned `0700` root-directory FD from the borrowed descriptor and holds a nonblocking exclusive advisory `flock` directly on it. The process-local registry rejects duplicate local ownership and makes close-and-registry-erase indivisible with fork; its `pthread_atfork` child handler closes inherited locked root FDs, while PID ownership rejects inherited ledger use and parent-registry acquisition with `ForkedProcess`. Move construction, assignment and destruction leave a foreign-PID ledger inert without acquiring an inherited mutex or closing a potentially reused child descriptor. The binding continues through `ValidatedBegin`, `LeaseStore`/`TerminalLease` and both sealed continuations, so source/destination writers reject inherited input and foreign ledgers before a callback, mutex or reservation. Debug ledger coverage passes 16 / 388; Release ASAN and UBSAN also pass ledger 16 / 388, `LeaseStore` 7 / 363, `ValidatedBegin` reuse 1 / 16 and `TerminalLease` reuse 1 / 58 without diagnostics. This coordinates participating helpers only. It neither protects the namespace from a noncooperating same-UID actor nor exposes or exercises a publication/cleanup operation or a stage-to-publication binding; `StagingPublicationBarrier` is now the separate reviewed root-acquisition/revalidation gate, while its publisher remains absent. The global VFS `TestDir` requires independent `VFSUT` processes to run sequentially; the recorded evidence did so.

- pre-runner standalone root-admission closure supersedes the earlier generic root-acquisition gap: helper-private `StagingRootAuthority` consumes only a current-process Commit `TerminalLease`, anchors two caller-borrowed root FDs, exact-validates root owner, `0700` mode and source/destination-parent device bindings, rejects same-device roots and acquires the ledger pair in canonical `(device,inode)` order. Its opaque move-only PID-bound `LockedSession` retains both locks and the terminal lease; a second-lock failure releases the first temporary lock. It exposes no FD, path, writer, publisher, cleanup or recovery operation. Before `StagingSessionRunner`, it was deliberately detached from `SourceSnapshotWriter → DestinationStageWriter`, because both paths consume the terminal lease; the runner now supplies only that private composition, while a publisher still needs separate exact revalidation/cancellation at its namespace barrier. Pre-runner Debug roots coverage passes 3 plus one expected missing-root skip / 111; the pre-runner full Debug `VFSUT` is 152 / 45,372 and the Debug helper target builds. Release ASAN and UBSAN each pass roots 3 plus one skip / 111, ledger 16 / 388, `LeaseStore` 7 / 363 and source snapshot/stage 9 plus one skip / 385 without diagnostics. The opt-in cross-device test requires `WINCOMMANDER_VFS_DESTINATION_STAGE_ROOT` and retains `.wc-staging-roots-ut.*` for manual inspection/removal only. The workspace, `/private/tmp` and `/Volumes` share one `st_dev`, and `/Volumes` has no mounted child, so that cross-device evidence was externally blocked; it is neither cleanup nor physical two-volume proof.

- locked-session runner closure before the barrier slice: helper-private `StagingSessionRunner` consumes `LockedSession` by value, rejects a foreign or moved session before either cancellation callback, retains both protected-root locks through `SourceSnapshotWriter::Create` followed by `DestinationStageWriter::Create`, and reports exact `Session`, `SourceSnapshot` or `DestinationStage` failure. The consumed session releases both locks on every return. Success returns only the existing move-only sealed read-only stage, after lock release; it grants no user pathname, stage-to-publication binding, publisher, cleanup or recovery authority. Its Debug roots coverage passed 3 cases plus 2 expected missing-environment skips / 111 assertions; Debug `VFSUT` then recorded 149 / 152 cases and 45,404 / 45,407 assertions, with exactly the known host baselines.

- historical private publication-barrier closure: helper-private `StagingPublicationBarrier::Prepare` consumes one current-process sealed destination stage, reacquires and retains the exact destination protected-root flock, and exact-validates the Commit terminal claim, source, destination parent, source snapshot, staged artifact, primary/sealed manifests, canonical artifact identity and destination absence before and after the sole unlocked cancellation callback. It fails closed on forked, moved, stale, busy-root, cancelled and destination-present inputs. Success returns only an opaque move-only permit retaining the read-only stage and root lock. It provides no descriptor, pathname, publication, cleanup or recovery operation and does not create, rename, link, unlink or open an entry in the user destination namespace. Its focused Debug roots coverage recorded 3 passed cases plus 3 expected missing-environment skips / 111 assertions; its full Debug `VFSUT` run recorded 149 / 152 cases and 45,368 / 45,371 assertions with exactly the `Application marker`, `FetchUsers` and `FetchGroups` host baselines; the Debug helper built. Fresh Release ASAN and UBSAN `VFSUT` builds linked their runtimes and each recorded roots 3 plus 3 skips / 111, ledger 16 / 388, `LeaseStore` 7 / 363 and source snapshot/stage 9 plus one skip / 385 without diagnostics. The later lifecycle aggregate is 161 / 164 and 46,560 / 46,563 with the same host baselines. The real stage-to-`Prepare` fixture needs `WINCOMMANDER_VFS_DESTINATION_STAGE_ROOT`, `F_FULLFSYNC` and a distinct device; it is skipped because `/`, `/private/tmp` and `/Volumes` resolve to `disk3`, `/Volumes/Macintosh HD` links to `/`, and no distinct mounted child exists. It is neither runtime publication, cleanup nor physical two-volume proof.

- current durable staged-lifecycle retention/inspection closure: while `StagingSessionRunner` still owns both locked ledgers, `StagingPublicationLifecycle::RecordStaged` accepts only the current-process Commit stage and exact-validates its terminal request, roots, snapshot/stage descriptors and sealed private artifacts. It writes one immutable lifecycle primary plus sealed companion on each root in canonical order. The two records bind header/correlation, both root `(device,inode)` identities, snapshot/stage IDs and seals, source/destination-parent seals and destination component; a partial record stays retained. `Inspect` reacquires both roots in canonical order and returns only `ExactPending`, `Absent`, `Incomplete`, `Mismatched` or `Malformed`. It never calls `ProtectedRootLedger::Reconcile()`, exposes no descriptor/path and issues neither cleanup/recovery execution nor publication authority. `ExactPending` requires both exact lifecycle record pairs and both exact sealed artifacts on distinct devices. Focused Debug lifecycle roots coverage passes 15 cases plus 3 fixture skips / 1,295 assertions; full Debug `VFSUT` records 161 / 164 cases and 46,560 / 46,563 assertions with only the `Application marker`, `FetchUsers` and `FetchGroups` host baselines; and the Debug helper target builds. Fresh Release ASAN and UBSAN `VFSUT` builds link their runtimes and sequentially pass that roots/lifecycle filter at 15 plus 3 fixture skips / 1,295, protected-root ledger 16 / 388, `LeaseStore` 7 / 363 and source snapshot 9 plus one fixture skip / 385 without diagnostics. A real staged source/destination pair followed by restart `Inspect(ExactPending)` still requires two distinct mounted internal writable APFS roots; this machine has only `disk3` for `/`, `/private/tmp` and `/Volumes`, while `/Volumes/Macintosh HD` links to `/`.

The final tree passes `git diff --check`.

## Remaining M3 gates

- execute the implemented internal/external physical-volume fixture and both physical checkpoint/recovery profiles with real abrupt-power hardware evidence; see `Docs/Features/reviewed_copy_as_physical_volume_protocol.md`;
- signed NonMAS artifact and SMJobBless trust proof; then either a descriptor-bound namespace primitive or a revised signed-root trust model with root-acquisition/transport proof before any publisher API, authority or production selection; helper-only cleanup/recovery execution and the two-internal-APFS physical proof; lifecycle inspection remains a retained-state classifier and `CrossVolumeStaged` remains `Unsupported`;
- [`OperationCenterModel`/`OperationId` control projection and persistence](operation_center_model_and_control_projection.md), plus broader mutation consumers.
