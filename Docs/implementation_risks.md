# Риски реализации

> Статус: local M0 baseline verified; active migration risk register
> Базовая ревизия аудита: `ec7f9f4`; mitigation status актуализирован 2026-08-03
> Связанные документы: [`win_commander_ideal_file_manager_spec.md`](win_commander_ideal_file_manager_spec.md), [`Development-Plan.md`](Development-Plan.md), [`feature_gap_matrix.md`](feature_gap_matrix.md)

## 1. Правила управления

Риск считается активным, пока mitigation подтверждён не только кодом, но и указанным evidence. Tripwire — наблюдаемое условие, после которого работа останавливается для исправления архитектурной или продуктовой границы.

Шкала:

- влияние: **critical** — риск потери данных/release blocker; **high** — ломает Alpha/Core contract; **medium** — создаёт существенный долг или деградацию;
- вероятность: **high**, **medium**, **low** относительно текущего baseline;
- владелец указывается как архитектурная область, а milestone — как крайний срок снижения риска.

## 2. Реестр

| ID | Риск и code evidence | Влияние / вероятность | Mitigation | Tripwire | Owner / deadline |
|---|---|---|---|---|---|
| R-01 | **Потеря данных из-за неполной mutation boundary.** Private reviewed authority, bounded internal-APFS publication, exact metadata/durability, lossless provider-result mapping, sealed execution product, exact receipt recovery, restricted cold hooks, exact durable-outcome delivery and production-configured Copy orchestration are implemented. Bounded production `CopyAs` adds exact review, stale-intent checks, process recovery and window submission custody. Its app seam proves zero enqueue for blocked/stale/unpersisted/cancelled intent and schedules the owning durable outcome before non-success Pool release. The journal can atomically retain an exact canonical terminal-evidence vector, but reviewed factory, production orchestrator, coordinator and `CopyAs` batch execution remain blocked; the only multi-item custody seam is test-only. An opt-in descriptor-anchored physical-volume fixture and test-only phase checkpoint harness are implemented; dedicated real-volume/hardware evidence and cross-volume staging remain open. | critical / high | Execute the physical-volume matrix and add provider-owned cross-volume staging before broader adoption. | A migrated app path mints review authority without a bound summary; UI dispatch loses the owning durable outcome; `Unknown` is collapsed; unavailable volume evidence enters execution; Pool releases before durable terminal write or exact reconciliation handshake. | Operations architecture / M3 |
| R-02 | **Расхождение authoritative state.** Path/listing/selection/sort распределены между `PanelController`, `PanelData` и view state. `PanelController` владеет stable `PaneId` и lifecycle coordinator; navigation и refresh используют request identity, explicit worker correlation, typed terminal state and post-model-commit publication. Production `PanelControllerPaneStoreAdapter` переиспользует identity и публикует committed/lifecycle state; Explorer breadcrumb, footer and Q1-7a folder overlay render only the matching active snapshot. Q1-7b additionally binds an exact committed Native listing generation to its mount identity before disconnect classification. Q1-7c binds inline rename to one exact controller listing/generation/item/provider/directory and discards the editor when the listing pointer changes. Q1-7d seals the drag source listing/generation/items and target listing/generation/item across one persistent Validate→Receive receiver; its pure policy owns exact modifier, path-capability, provider and Native-volume facts, and complete receive-time decision equality gates enqueue. Each Explorer runtime tab owns a distinct controller, Store adapter, `PaneId` and History; one generation+`PaneId` observation token binds the active tab to its chrome projection. Q1-5 owns exact per-location view settings. Q1-6 captures only exact canonical Store locations and recreates fresh controller/Store/History identities, leaving view resolution to Q1-5; remaining row and operation consumers retain mixed ownership. | high / medium | Сохранить Store адаптером над текущим model, определить единственного writer для каждого поля и переводить consumers через тот же lifecycle/Store contract. | Одно поле path/loading/selection/view mode изменяется из двух независимых owners; drag receive accepts different listing/item/policy facts than its badge; consumer выводит loading/failure из queue occupancy, callback timing или view notification. | Pane architecture / M1-M2 |
| R-03 | **Main-thread stalls и деградация больших папок.** Listing загружается асинхронно, но `PanelData::Load` строит полную модель и сортировки, а `MakeNew.mm::HasEntry` сканирует listing. M0 model baseline даёт 56.8585 ms для 10k и 753.492 ms для 100k, но не измеряет VFS, first render, scrolling, memory или production-thread scheduling. Thumbnail/preview paths имеют отдельные очереди без общего budget. | high / high | Сохранить model benchmark как regression anchor; до M2 rendering changes добавить signposts и p50/p95 end-to-end measurements; затем вводить incremental presentation/virtualization boundary, cancellable metadata и bounded caches по данным профиля. | >100 ms main-thread stall от UI action; 10k не показывает первые строки до полной metadata; 100k блокирует scroll/selection; unbounded thumbnail growth. | Performance / M2, M7 |
| R-04 | **Неверная provider semantics.** Conservative capabilities/planning remain fail closed. Native conditional Copy consumes private-sealed claims, validates canonical exact-child paths, exact host, kind/mode, identity/size and nanosecond timestamps, anchors descriptors and admits only internal/local/writable APFS with exact required capabilities. One `fclonefileat(..., CLONE_ACL)` owns exclusive publication; metadata/name verification and ordered durability follow it; commit/abort caches `NotPublished`/`Published`/`Unknown`. `SameVolumeClone` is now distinct from an unactivated `CrossVolumeStaged` seam; it forwards only anchored FDs, basename, complete scalar seals and the synchronous cancellation checker before publication, and has no client cleanup authority. `CrossVolumeStagingProtocol` validates a separate versioned claim/lease/completion ABI and keeps descriptor transfer in a forthcoming private codec rather than the legacy generic helper. The physical fixture requires a real external APFS root to prove `UnsupportedExternalMedia`; its run and hardware power-loss evidence remain outstanding. | critical / high | Preserve `Unsupported`/`Unavailable`/`Unknown`; keep the ADR 0001 clone-only scope bounded; require ADR 0002 helper-owned lease/recovery before advertising staged scope; execute the protocol in `Docs/Features/reviewed_copy_as_physical_volume_protocol.md` before consumer expansion; compose richer provider identity only from authoritative contracts. | A non-clone path is presented as equivalent authority; a helper request carries a stage/cleanup path; post-publish durability failure is erased; local clone assumptions leak into remote/archive execution. | VFS/Commands / M1, M3, M6 |
| R-05 | **Невосстановимая или ошибочно продолженная операция после restart.** Journal issues exact admission/run receipts and atomically persists a complete canonical terminal-evidence snapshot plus terminal state; custodian preallocates exact plan/storage ownership before Running, retains immutable retry evidence, and requires same-storage reopen reconciliation after rename uncertainty. Pool-owned slots remain resident until durable confirmation; reconciled slots use an exact `ReleaseReconciled` Pool probe/erase handshake. Startup converts admitted/running work to `Interrupted` without auto-resume. The coordinator can append only absent terminal/interrupted records from that exact storage while it is cold, and explicit confirmed recovery invokes it only after its custody service returns and required Pool release succeeds. Exact `ColdHistoryBusy` creates a one-use opaque storage-identity/consumed-bit projection token; its only UI action calls current-journal `RefreshColdHistory`, never custody retry, reopen, reconcile or release. The physical power-loss protocol requires descriptor-bound read-only capture before `OperationJournal::Open`, then destination inspection before any retry or cleanup. | critical / medium | Preserve the bounded one-shot history refresh and exact custody/storage/release gates; add full history presentation independently. | Reconcile accepts a different journal; refresh runs with a live residency or draft; Pool release is inferred from a callback rather than exact residency; interrupted work auto-resumes; history cannot explain tri-state publication/recovery. | Persistence/Operations / M3, M7 |
| R-06 | **Потеря или повреждение пользовательского state при миграции схемы.** Q1-5 owns a separate schema-v1 view leaf with complete canonical locations and bounded deterministic MRU. Q1-6 adds a strict schema-v1 window envelope around the intact Commander payload, reads legacy `panels_v1` roots and writes the new form, selects Cocoa → `StateConfig` → default, preserves a stored future schema, limits Explorer to 64 tabs and normalizes one malformed tab to Home without discarding its siblings. Structural restore failure rolls back the Explorer topology and retains the Commander base. | medium / low | Keep canonical read-old/write-new fixtures, future-schema preservation, atomic Config writes and per-tab corruption fallback in the focused gate. | Persistent key/schema changes without migration coverage; a higher future schema is overwritten; decode or topology failure exposes an empty/partial window. | Settings/Persistence / M1-M2, M7 |
| R-07 | **Security/signing regressions между Unsigned, NonMAS и MAS.** Targets имеют разные schemes/entitlements; permission, security-scoped access, Admin Mode и network behavior не эквивалентны. Deterministic policy now proves normalized bookmark containment and no-fetch/no-prompt failure paths (4 / 47), but it cannot prove a signed sandbox entitlement, clean bookmark consent or TCC/FDA behavior. Local development now has one canonical signed runtime with separately pinned certificate fingerprint/exact requirement, entitlement verification, serialized replacement and rollback; the historical unsigned-run entry point delegates to it. The current machine's certificate predates restricted import and retains the legacy permissive `-A` key ACL; changing that custody requires a separate fingerprint-preserving migration. Keychain уже используется для network passwords, но новый diagnostics/state layer может случайно сериализовать credentials или private paths. | critical / medium | Составить variant capability matrix; использовать canonical stable runtime для local app-bound checks; сравнивать exact designated requirement/entitlements после signing/target/plist changes; отдельно спроектировать и проверить migration legacy local-key ACL без изменения fingerprint; оставить secrets в Keychain; redaction at source; security tests для logs/export/persistence; separately verify signed MAS clean-bookmark consent and FDA/TCC with the release identity. | App-bound evidence получено из DerivedData/adhoc artifact; local identity/requirement pin или bundle ID меняется неявно; TCC-trusted local private key остаётся с broad ACL; secret/password/key material появляется в Config, operation journal или diagnostics; функция работает только в unsigned build без documented gate; entitlement расширяется без review. | Platform/Security / M0, M6-M7 |
| R-08 | **Silent regression при неполном hosted CI gate.** Current full Debug results are `OperationsUT` 216 / 5,726, `VFSUT` 179 / 46,786 and `WinCommanderUT` 580 / 10,147. Q1-9 focused Search passes 62 / 1,010 in Debug, Release ASAN and Release UBSAN, focused `SearchForFiles` passes 9 / 68 and Debug `WinCommander-Unsigned` builds; both sanitizer executables link their runtimes without diagnostics. Q1-8 progress/current-item sanitizer and app-build closure remains recorded in its feature spec. Hosted required checks, signed VoiceOver/Spotlight/FDA/long-copy/conflict walkthroughs, external-drive UI and physical-volume integration remain open. | high / medium | Publish hosted all-module evidence, make the workflow mandatory, add documentation checks and split deterministic integration from credential/volume suites. | Shared module merges without green current-tree/hosted gate; focused compile is reported as the pending full suite; historical count is reported as current; skipped physical profile is reported as physical proof; flaky test is disabled without owner/expiry; production provider path has only unit authority. | Build/Test / M0, M7 |
| R-09 | **Upstream drift и дорогое слияние с базовым проектом.** Fork сохраняет крупные upstream-derived modules и copyright; настроен только `origin`, отдельный upstream remote/merge cadence не зафиксирован. Широкие переписывания shared files увеличат конфликты и риск пропустить fixes. | high / medium | Зафиксировать upstream remote и cadence; держать новые contracts в узких adapter files; регулярно делать dry-run merge/rebase report; не смешивать rename/formatting с behavior changes; сохранять GPL notices. | Более одного milestone без upstream audit; массовый rewrite shared module; copyright/license header удалён; merge conflict затрагивает operation safety без dedicated tests. | Maintainers / M0, затем каждый milestone |
| R-10 | **Accessibility становится поздним redesign.** Explorer's established command, pane, inspector, tab, rename and drag surfaces retain semantic presentation. Q1-8 exposes the progress strip as one labelled group whose value composes title, full current path, lifecycle, progress, rate, ETA and additional count, posts value-change notifications and pairs state text with the native indicator. The conflict sheet has stable identifiers/labels for compared metadata and every action, with native destructive styling for Replace and batch-only Apply to all authority. Unified row focus/selection, terminal item/error/result announcements, suggested actions and signed manual VoiceOver evidence remain open. | high / high | Расширять проверенные command/pane presentation patterns на каждый P0 slice; semantic labels/roles/values/actions рядом с UI implementation; keyboard and VoiceOver tests; цвет всегда дублируется текстом/иконкой. | Новый interactive control без label/keyboard path; drag badge/count is not exposed by the signed runtime; disabled reason расходится между surfaces; focus теряется после reload; error/progress не объявляется. | UI/Accessibility / M2, M4-M7 |
| R-11 | **Scope explosion блокирует Core.** M1 foundations, production navigation/refresh, reviewed Copy boundaries and bounded Operation Center controls exist. Q1-8 adds only a copied exact per-window Pool progress adapter plus the established conflict sheet; it preserves executor, journal and persistent-history ownership. The full live Center remains separately scoped to terminal result/error/log/history and sealed controls. | high / high | Route work through `Development-Plan.md`; allow independently safe, authority-free presentation slices, but require the physical-volume contract, reviewed provider authority and cross-volume publisher gates before execution adoption or broader mutation consumers. | The bounded progress strip is presented as complete Operation Center migration; a command surface bypasses registry/state/error/presentation; progress polling gains executor authority. | Product/Architecture / continuous, Alpha |
| R-12 | **Регрессия Commander mode при миграции Explorer.** Explorer переиспользует `PanelController`, dispatcher, VFS, Operations, `PanelView` drag infrastructure and the established `FilePanelsTabbedHolder`. Commander retains its established holder/controller ownership, while Explorer keeps its ordered tab model and per-tab Store/History set inside `NCExplorerState`. Q1-5 marks concrete Explorer presentations as pane-local. Q1-6 embeds the validated legacy Commander state unchanged in every envelope, restores it before Explorer and keeps it as the atomic fallback when Explorer topology construction fails. Copy/Cut/Paste сохраняют shared Edit responder precedence and menu cleanup; Trash/Permanent Delete retain established selectors, shortcuts and legacy operation dialogs. Q1-7d changes shared local drag admission but continues constructing established `Copying`/`Linkage`; exact source/target seals and identical policy re-evaluation apply in both modes. Full `PanelUT` records 54 / 1,349 total with 53 / 1,348 passed plus one expected host-UI failure / one assertion and exit 0; Debug `WinCommander-Unsigned` builds successfully. | medium / low | Preserve the Commander envelope fixture, restore-first ordering, shared-menu/drag characterization and dual-mode smoke at Queue 1 gates. | Shared drag behavior changes without Commander regression evidence; validated receiver is replaced before receive; session decode mutates Commander before source validation; Explorer failure leaves a partial state stack. | App Shell/Panel / M1-M3, M7 |
| R-13 | **Потеря смысла ошибок при адаптации.** Lossless POSIX `FileManagerError` baseline сохраняет original error, category, messages и context. VFS planning adapter классифицирует Host failures, включая SFTP domain, в typed probe/blocker outcomes; navigation failure проходит через `PaneState::visible_error` and `VisualStateMapper`. Q1-7a renders the resulting permission, missing-path, volume, remote, unsupported and generic pane kinds with the mapped user message while retaining committed content on refresh failure. Q1-7b classifies a disconnected Native volume only when the exact mount identity bound to the committed listing generation is absent; unknown membership preserves the original failure, and same-path remount identity is distinct. Remaining provider domains, terminal and partial-failure mappings remain separate boundaries. | high / medium | Расширять typed Store/presenter path без text-based inference; сохранять provider/operation/item context и result source; unknown category направлять в redacted technical log; проверять recovery actions через Registry. | Planner blocker теряет path/category при presentation; ошибка преобразуется только в localized string; retry определяется текстом; partial failure отображается как success или один общий failure. | Error Model / M1, M3, M6 |
| R-14 | **Race, stale state и зависший provider при реактивной миграции.** Production navigation/refresh use stable request identity, explicit worker slots, cancellation, exact identity/epoch/source gates and post-model-commit Store reduction. Explorer tab activation invalidates the prior observation token before rebinding chrome, and tab close removes the model identity before detaching UI. Q1-7d applies the same fail-closed pattern synchronously to drag/drop through exact source/target listing, generation, item and policy equality. Q1-9 applies a pane-bound monotonic run ID plus exact origin listing/data-generation fences to Search Mode; a superseding run, external content replacement, cancellation, tab close or controller teardown rejects late progress/completion. Backend Stop is nonblocking on main and a detached reaper owns Wait/destruction through weak callbacks. The bounded result builder commits no listing after cancellation, and Reveal accepts only the focused item from the controller's exact committed result listing. Focused Search passes 62 / 1,010 in Debug, Release ASAN and Release UBSAN and full Debug `WinCommanderUT` passes 580 / 10,147; `SearchForFiles` passes 9 / 68 and full Debug `VFSUT` passes 179 / 46,786. Both sanitizer executables link their runtimes without diagnostics. A non-returning provider can still retain a detached executor/request indefinitely without retaining the pane or blocking main. Real Native and Docker navigation/refresh/outage evidence remains recorded below; signed FDA/security-scoped, physical external-drive UI and temporary-listing fixtures remain outside this E2E. | high / medium | Add bounded provider cancellation/deadline contracts where supported and observability for orphaned requests; retain the same exact identity/cancellation contract in preview and operations; complete signed live-provider/FDA evidence. | Stale/cancelled result commits; reveal targets another listing; drag receive accepts different source/target/policy facts; controller teardown blocks; detached requests grow without bound; provider feedback loses source. | Stores/Concurrency / M1-M4 |
| R-15 | **Недостаточная диагностика или утечка данных в диагностике.** Есть module logging и dialogs, но нет единого operation/search/provider correlation ID и экспортного redaction contract. Без этого fault reports нерепродуцируемы; при чрезмерном логировании раскрываются paths/content. | high / medium | Structured local events с correlation IDs и category; allowlist fields; path redaction; user-initiated export; tests, что secrets/file contents отсутствуют. | Failure невозможно связать с command/plan/provider; diagnostics содержит password/token/private file contents; exported report формируется автоматически. | Diagnostics/Security / M3, M6-M7 |
| R-16 | **Pasteboard rollback не атомарен после очистки системного clipboard.** `file.cut` валидирует native payload до clear и публикует Move marker/snapshot только после marker success; marker failure оставляет safe Copy state. Public pasteboard API не позволяет атомарно восстановить arbitrary external or lazy types после post-clear OS failure. | high / low | Сохранять pre-clear validation и fail-safe Copy fallback; считать marker/snapshot единственным app-owned cut token; добавить fault-injection coverage для каждой доступной write boundary и ясно отделять clipboard intent от filesystem mutation. | Failed cut оставляет активный Move token; предыдущий clipboard объявляется восстановленным без доказательства; paste mutates files без `OperationPlan`. | Commands/Platform / M1-M3 |
| R-17 | **Накопление staging artifacts или потеря recovery authority.** Native capsule can retain an artifact after hostile same-UID rebind. POSIX validation plus `renameatx_np(..., RENAME_EXCL)` cannot bind a named stage inode to publication, and journal item results lack descriptor-bound cleanup identity. ADR 0002 `ProtectedRootLedger` retains exact primary reservation `R`, private artifacts and append-only companion `S`; reconciliation classifies sealed, absent, incomplete, malformed and mismatched state read-only. Sixteen canonical primary records, including malformed records, form the durable quota and restart rejects a duplicate header/role claim. It now holds a nonblocking exclusive advisory `flock` directly on its exact helper-owned `0700` root FD; its process registry rejects duplicate owners and makes close-and-registry-erase indivisible with fork, while `pthread_atfork` closes inherited locked root FDs and PID validation rejects inherited ledger use and parent-registry acquisition. Move construction, assignment and destruction leave foreign-PID ledgers inert without taking inherited mutexes or closing a child-reused descriptor number. Detached helper-private `StagingRootAuthority` consumes a current-process Commit terminal lease, anchors two borrowed roots, exact-validates their owner/mode/device bindings, rejects same-device roots and retains canonical `(device,inode)` locks only in a PID-bound opaque `LockedSession`; it exposes no writer, publisher, cleanup or recovery capability, and a second-lock failure releases the first temporary lock. `StagingSessionRunner` consumes that session and composes the snapshot and destination-stage writers while both root locks are held; it returns only the sealed read-only stage after the locks release. `StagingPublicationBarrier` then consumes that stage, reacquires the exact destination-root lock and exact-revalidates the terminal claim, source, parent, snapshots/stage, durable records and destination absence before and after its sole cancellation callback. Its opaque permit retains the read-only stage and root lock but performs no user-namespace operation. This remains participating-helper coordination: it does not prevent a noncooperating same-UID actor from mutating the namespace, bind a staged inode to final publication, or issue publisher, cleanup or recovery authority. `SourceSnapshotWriter` admits only a source root on `request.source.device`; its Commit-only continuation exposes no path/write FD and cannot survive restart. `DestinationStageWriter` consumes only that continuation, requires a destination root on `request.destination_parent.device` distinct from the source device, revalidates snapshot/source/parent/root/reservation after every unlocked cancellation probe, and creates only a private sealed `0600` stage. Its continuation retains private read-only descriptors but exposes or exercises no publication or cleanup operation. The opt-in root-admission fixture retains `.wc-staging-roots-ut.*` only under an explicitly supplied disposable root; that is inspection-only, not cleanup or physical proof. A publisher consuming the permit, helper-owned cleanup/recovery and exact destination publication remain the authority needed to close the accumulation risk. | high / medium | Keep cross-volume reviewed Copy on the established legacy route until a reviewed publisher consumes the locked permit for exact atomic namespace publication, bounded recovery ownership exists and publication is durably classified; apply security metadata within that authority and expose retained artifacts without path-pattern deletion. | Temporary files grow without accounting; cleanup unlinks an unsealed name; ACL/time/flags are applied through a public destination race; advisory coordination is presented as same-UID filesystem protection; journal prescribes cleanup without actionable authority. | Operations/Persistence / M3, M7 |

Current R-17 lifecycle addition: `StagingPublicationLifecycle::RecordStaged` writes immutable cross-bound primary/sealed lifecycle pairs only while the private runner holds both protected roots. It validates the Commit request, root identities, snapshot/stage descriptors and their sealed artifacts; its records bind correlation, roots, artifact IDs/seals, original source/destination-parent seals and destination component. Partial state is retained. Its separate canonical-locking `Inspect` returns `ExactPending`, `Absent`, `Incomplete`, `Mismatched` or `Malformed`, never calls `ProtectedRootLedger::Reconcile()` and grants no cleanup, recovery-execution or publisher authority. `ExactPending` requires both exact lifecycle pairs and sealed artifacts on distinct devices. Focused Debug lifecycle roots coverage passes 15 cases plus 3 fixture skips / 1,295 assertions; full Debug `VFSUT` records 161 / 164 and 46,560 / 46,563 with only the `Application marker`, `FetchUsers` and `FetchGroups` host baselines; and the Debug helper target builds. Fresh Release ASAN and UBSAN `VFSUT` runs link their runtimes and sequentially pass roots/lifecycle 15 plus 3 fixture skips / 1,295, protected-root ledger 16 / 388, `LeaseStore` 7 / 363 and source snapshot 9 plus one fixture skip / 385 without diagnostics. No two-device restart inspection proof is claimed.

R-17 PID boundary: `ValidatedBegin`, `LeaseStore`, each `TerminalLease`, both sealed continuations, `StagingRootAuthority::LockedSession` and the publication permit are bound to their creating helper process. The source and destination writers reject inherited terminal or continuation input before taking a ledger mutex or reserving a private artifact; detached root admission rejects an inherited terminal before it touches borrowed root FDs, `StagingSessionRunner` rejects an inherited or moved session before either callback, and `StagingPublicationBarrier` rejects inherited or moved stages before a callback, ledger acquisition or descriptor revalidation. This prevents a fork child from extending the parent's in-memory authority; it does not change the same-UID namespace or final-publication boundary.

Current R-17 barrier closure follows the runner: `StagingPublicationBarrier` consumes the read-only stage, reacquires and retains the exact destination protected-root flock, exact-revalidates root/stage/source/parent/records/destination both before and after its sole cancellation callback, and returns only an opaque nonmutating permit. It does not bind the staged inode to a user name, publish, clean up or recover. A named stage plus `renameatx_np(..., RENAME_EXCL)` cannot make that binding, and `0700` roots, `O_NOFOLLOW`, descriptor rechecks and `flock` leave a same-UID substitution window. The available environment has no real euid-0 signed root-isolation proof. Only a `VFSUT`-only characterization may demonstrate the window; no `StagingPublisher` API, helper authority or production selection is permitted until a descriptor-bound namespace primitive exists or a revised signed-root trust model has proven root acquisition and transport. `CrossVolumeStaged` remains `Unsupported`; helper-owned cleanup/recovery is separately required. The distinct-device stage-to-barrier fixture is externally blocked: `/`, `/private/tmp` and `/Volumes` use `disk3`, `/Volumes/Macintosh HD` links to `/`, and no distinct mounted child is available; its retained `.wc-staging-roots-ut.*` root is inspection-only.

Recorded R-14 transport update: the remote slice had Debug `VFSUT` 95 / 43,563 and `WinCommanderUT` 321 / 5,206; `IntegrationTests` built, SFTP blackhole VFSIT passed 1 / 2, and focused Docker remote navigation passed 9 / 376. FTP listing, WebDAV blocking-control and SFTP TCP/session paths have 30-second production deadlines plus isolated 500 ms seams. A paused live endpoint preserves raw FTP `operation_timeout`, SFTP `timeout` and WebDAV `POSIX/ETIMEDOUT`, maps each to `TimeoutError`, retains the prior listing/generation and reconnects through the same controller after unpause. Negative SFTP `readdir` fails closed and unavailable/timeout connections are discarded rather than returned to the pool. Latest full Debug evidence records `VFSUT` 161 / 164 and 46,560 / 46,563 with only the `Application marker`, `FetchUsers` and `FetchGroups` host baselines, and `WinCommanderUT` 330 / 334 and 5,317 / 5,321 with four headless pasteboard host baselines. FDA/security-scoped and temporary-listing fixtures remain outstanding acceptance evidence.

Current R-18 control update: all twenty-seven stable IDs have production Registry definitions. Q1-1/Q1-2 roster and surface composition are complete. New File uses the narrow `EmptyFileCreation` boundary; Archive Extract admits typed sealed acquisition and a bounded validated manifest before established `Copying`; read-only Get Info stays separate from mutating File Attributes. Q1-3 adds exact Get Info, exact Quick Look and checked compare-and-set details visibility across menu, persisted shortcut, exact context and Explorer surfaces. Focused command coverage is 17 / 288, dispatcher 1 / 46, Inspector/AppKit 8 / 111 and model 6 / 97; full Debug `WinCommanderUT` records 420 / 425 and 7,435 / 7,439 with four known headless pasteboard baselines and one expected skip. Batch Rename retains its documented legacy no-overwrite/journal/undo risk; the full live Operation Center remains open.

Current R-07 permission update: `SandboxManager::PathIsWithinScope` accepts only an exact normalized bookmark scope or a component descendant, closing the `/scope` → `/scope-other` prefix escalation. `PanelController` lifecycle tests pass 4 / 47 for denial, automatic no-prompt behavior and user grant; denial reports one Admission callback and cannot fetch or replace the committed model. Latest full Debug `WinCommanderUT` records 330 / 334 and 5,317 / 5,321 with four headless pasteboard host baselines. This evidence does not replace signed MAS clean-bookmark consent or FDA/TCC release checks.

**Latest aggregate update (2026-08-07):** this supersedes earlier current-rerun figures above: Debug `VFSUT` passes 179 cases / 46,786 assertions; Debug `OperationsUT` passes 216 / 5,726; Debug `WinCommanderUT` passes 580 / 10,147. Q1-9 focused Search passes 62 / 1,010 in Debug, Release ASAN and Release UBSAN, focused `SearchForFiles` passes 9 / 68 and Debug `WinCommander-Unsigned` builds; both sanitizer executables link their runtimes without diagnostics. Q1-8 sanitizer/app evidence remains recorded in its feature spec.

Current M3 physical-checkpoint update: the test-only `ConditionalCopyIO` seam records `BeforePublish` and `AfterPublishBeforeFullFSync`. The hidden physical profile requires an explicit phase, writes and synchronizes its manifest before block mode retains the workspace, and injects the selected `NativeHost` through the physical review bindings. The manifest records run ID, phase, capture time, journal file plus parent descriptor identities, source/destination identities and filenames. A manifest or retention failure propagates through Clone or `F_FULLFSYNC`; Native conditional-Copy tests pass 17 / 347 and Debug `OperationsIT` builds. The risk gate still requires a dedicated internal/external APFS execution and physical power-loss/reboot evidence, including the pre-open journal capture and destination inspection.

## 3. Обязательные risk gates

### M1 baseline

- Определены owners и writers для app/pane/folder/selection/search/operation state.
- Зафиксированы compatibility seams: dispatcher→Registry, PanelController/PanelData→PaneStore, VFSHost→ProviderCapabilities/planner probes, `nc::Error`→FileManagerError, accepted plan→Operation/Pool→Center.
- Локальный M0 gate воспроизводит unsigned Debug build и все 10 aggregate unit binaries; hosted CI и required-check policy остаются открытыми.
- Снят воспроизводимый 10k/100k model baseline; end-to-end UI/main-thread trace обязателен до M2 rendering changes.

### До первого P0 mutation на новых contracts

- Plan создаётся и сериализуется до enqueue; execution starts only from an exact journal-bound admission receipt.
- Bound production preflight проверяет provider capability, path identity, destination и применимые destructive effects, удерживая exact bindings. Application permission composition дополняет evidence; `Accepted` проходит отдельный review/confirmation policy до factory.
- Production provider conditional transaction, typed result mapping/execution product, private factory/orchestrator integration, restricted cold hooks, exact durable-outcome delivery and run-receipt recovery are proven. Bounded `CopyAs` supplies exact app review/presentation and has focused zero-enqueue/durable-dispatch boundary proof; physical-volume evidence and cross-volume staging remain separate gates.
- Fault injection покрывает source/destination/temporary-name rebind, permission revoke, disk full, metadata failure и cancel.
- Typed tri-state outcome is published atomically before Pool release; terminal-transition authority is preallocated before start; `Retain`/retry covers runtime persistence faults; `ReleaseWithoutCompletion` separates durable non-success from generic completion; reconciled residency uses `ReleaseReconciled`; post-Running pre-enqueue recovery is durable; and retained artifacts have bounded descriptor-bound authority.

### До Alpha

- Все critical/high tripwires закрыты либо имеют явно принятый release blocker.
- Commander и Explorer проходят общий regression slice.
- Large-folder, accessibility, persistence/recovery, permission и visual-state evidence приложены к gate.
- Нет silent failures, direct UI/VFS mutations и неразъяснимых disabled actions.

## 4. Порядок снижения рисков

При конфликте сроков порядок жёсткий: data safety → correctness/state ownership → responsiveness → visual/error transparency → accessibility → feature breadth. Оптимизация количества surfaces до стабилизации boundary tests является ложной экономией: она размножает несовместимые entry points и увеличивает стоимость каждого последующего исправления.
